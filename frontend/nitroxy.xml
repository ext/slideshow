<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE template SYSTEM "slide_template.dtd">
<template>
	<background fit="scale">../nxgroen.png</background>
	<font name="font" default="true">/home/ext/slideshow-src/frontend/Eurostile.ttf</font>

	<!--
		Units:
		All units defaults to pixels, if the unit is suffixed
		with % it is relative to the size of the slide

		Negative units can be used to count from the opposite
		side of the slide.

		For instance, an x coordinate of:
		 * 50;		50 pixels from the right
		 * -50;		50 pixels from the left
		 * -5%;		5% of the screen from the left
	-->

	<!--
		Colors:
		There is three ways to select a color:
		 * Decimal RGB[A]: "255, 0, 135"
		 * Hexadecimal RGB[A]: "#FF00CC"
		 * Named color: "red"
	 -->

	<!--
		Valid field types:
		 * text: Single line of text
		 * textarea: Multiple lines of text
		 * static: Insert static text at given position
	 -->
	<fields>
		<text name="title" font="font" size="36" color="#18ff1c" align="center" position="50% 50" />
		<textarea name="content" font="font" color="#18ff1c" size="48" align="center" boxsize="90% 80%" position="50% 47%" />
	</fields>

</template>
