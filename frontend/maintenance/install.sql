
CREATE TABLE `files` (
  `id` int(11) NOT NULL auto_increment,
  `fullpath` text,
  PRIMARY KEY  (`id`)
);

CREATE TABLE `settings` (
  `id` int(11) NOT NULL auto_increment,
  PRIMARY KEY  (`id`)
);

